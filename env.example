# 姓名判断アプリ - 環境変数設定例
# 本番環境では Vercel Dashboard → Settings → Environment Variables で設定

# Square 決済設定
# 環境切り替え: SQUARE_ENVIRONMENT=sandbox または SQUARE_ENVIRONMENT=production
# 未指定の場合は、NODE_ENV=production かつ SQUARE_ACCESS_TOKEN が設定されている場合に本番環境
SQUARE_ENVIRONMENT=sandbox
# SQUARE_ENVIRONMENT=production  # 本番環境に切り替える場合

# 本番環境用（SQUARE_ENVIRONMENT=production の場合）
SQUARE_APPLICATION_ID=sq0idp-wGVapF8sNt9PLrdj5znuKA
SQUARE_ACCESS_TOKEN=EAAAExxxxxxxxxxxxxxxxxxxxxx
SQUARE_LOCATION_ID=LMxxxxxxxxxxxxxx
SQUARE_WEBHOOK_SIGNATURE_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Square側で作成したサブスクリプションプランのID（Subscriptions APIを使用する場合）
# Squareダッシュボード > 商品とサービス > サブスクリプションプラン で確認
SQUARE_SUBSCRIPTION_PLAN_ID_BASIC=your_basic_plan_id_here
SQUARE_SUBSCRIPTION_PLAN_ID_PREMIUM=your_premium_plan_id_here

# Square Payment Links（外部決済用）
# Squareダッシュボード > オンライン決済 > 支払いリンク で確認
# 現在設定されているリンク:
NEXT_PUBLIC_SQUARE_PAYMENT_LINK_BASIC=https://square.link/u/6sJ33DdY
NEXT_PUBLIC_SQUARE_PAYMENT_LINK_PREMIUM=https://square.link/u/TjSKFJhj

# Google Play Billing（Androidアプリ内課金用）
# Google Play Console > 収益化 > 商品 > サブスクリプション で作成した商品ID
NEXT_PUBLIC_GOOGLE_PLAY_PRODUCT_ID_BASIC=basic_monthly
NEXT_PUBLIC_GOOGLE_PLAY_PRODUCT_ID_PREMIUM=premium_monthly
# Google Play Console > 収益化 > 商品 > アプリ内商品 で作成した商品ID（一回限りの購入）
NEXT_PUBLIC_GOOGLE_PLAY_PRODUCT_ID_DRAGON_BREATH=dragon_breath
# Google Play Developer API用のサービスアカウントキー（JSONファイルのパスまたはJSON文字列）
# サーバー側での購入検証に使用
# 方法1: ファイルパスを指定
GOOGLE_PLAY_SERVICE_ACCOUNT_KEY_PATH=./google-play-service-account-key.json
# 方法2: JSON文字列を直接設定（Vercelなどの環境変数で推奨）
# GOOGLE_PLAY_SERVICE_ACCOUNT_KEY_JSON={"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}
# アプリのパッケージ名（Google Play Consoleで確認）
# NEXT_PUBLIC_GOOGLE_PLAY_PACKAGE_NAME=com.kanau.nameanalysis

# フロントエンド用（公開可能）
# Sandbox環境の場合: sandbox-sq0idb-xxxxx
# Production環境の場合: sq0idp-xxxxx
NEXT_PUBLIC_SQUARE_APPLICATION_ID=sq0idp-wGVapF8sNt9PLrdj5znuKA
# Sandbox環境の場合: NEXT_PUBLIC_SQUARE_APPLICATION_ID=sandbox-sq0idb-xxxxx
NEXT_PUBLIC_SQUARE_LOCATION_ID=LMxxxxxxxxxxxxxx
# Sandbox環境の場合: NEXT_PUBLIC_SQUARE_LOCATION_ID=LHxxxxxx

# 分析・監視
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX
NEXT_PUBLIC_CLARITY_PROJECT_ID=clarity-project-id

# 本番環境フラグ
NODE_ENV=production

# セキュリティ設定
NEXTAUTH_SECRET=your-secret-key-here
NEXTAUTH_URL=https://your-domain.com

# データベース（オプション）
DATABASE_URL=your-database-url-here

# メール送信 - Resend（推奨）
# Resend: https://resend.com/ でAPIキーを取得
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# ⚠️ 現状の運用: ドメイン認証が完了していないため、以下の設定で運用しています
# 送信元メールアドレス: onboarding@resend.dev（固定、変更不要）
# 管理者への通知先: Resendアカウントのメールアドレス（sasayan.biz@gmail.com）にのみ送信可能
# ユーザーへの自動返信: 現状では送信不可（ドメイン認証完了後に有効化）
# 送信元メールアドレス（ドメイン認証が完了したら有効化）
# RESEND_FROM_EMAIL=noreply@seimei.app
# お問い合わせ先メールアドレス（管理者への通知先）
# ⚠️ 重要: ドメイン認証が完了していない場合、Resendの未認証ドメインでは
# 自分のメールアドレス（Resendアカウントのメールアドレス）にのみ送信可能です。
# 現状の運用: kanaukiryu@gmail.com に設定
CONTACT_EMAIL=kanaukiryu@gmail.com
# 自動返信メールを送信するか（falseで無効化）
# ⚠️ 現状の運用: ドメイン認証が完了していないため、自動返信は送信不可
# フォーム送信自体は成功として扱われ、ユーザーには「お問い合わせを受け付けました」と表示されます
SEND_AUTO_REPLY=false

# メール送信（旧方式・オプション）
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Optional, server-side only
SUPABASE_SERVICE_ROLE_KEY=

# X (Twitter) API設定（自動投稿用）
# テキストのみのツイート: Bearer Tokenのみで十分（推奨）
TWITTER_BEARER_TOKEN=your_bearer_token_here
# 画像付きツイート: OAuth 1.0aの4つの認証情報が必要（TWITTER_API_KEY, TWITTER_API_SECRET, TWITTER_ACCESS_TOKEN, TWITTER_ACCESS_TOKEN_SECRET）
# 画像付きツイートを投稿する場合は、以下の4つも設定してください
TWITTER_API_KEY=your_api_key_here
TWITTER_API_SECRET=your_api_secret_here
TWITTER_ACCESS_TOKEN=your_access_token_here
TWITTER_ACCESS_TOKEN_SECRET=your_access_token_secret_here

# Vercel Cron 認証（オプション）
CRON_SECRET=your_cron_secret_here

# 開発・テスト用（本番環境では設定しないこと）
# URLパラメータ（/?plan=premium等）によるプラン切り替えを有効化
# 本番環境では常に無効化されるため、この設定は不要です
# NEXT_PUBLIC_ALLOW_PLAN_PARAM=true

# Firebase Firestore（言霊マスターデータ用）
# 方法1: サービスアカウントキーのファイルパスを指定
FIREBASE_SERVICE_ACCOUNT_KEY_PATH=./secret/firebase-service-account-key.json
# 方法2: JSON文字列を直接設定（Vercelなどの環境変数で推奨）
# FIREBASE_SERVICE_ACCOUNT_KEY_JSON={"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}

# Google Gemini API（AI鑑定機能用）
# Google AI Studio (https://makersuite.google.com/app/apikey) でAPIキーを取得
GOOGLE_GENERATIVE_AI_API_KEY=your_gemini_api_key_here